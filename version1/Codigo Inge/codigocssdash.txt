/* Dashboard Variables */
:root {
    --sidebar-width: 280px;
    --header-height: 70px;
    --card-padding: 20px;
    --card-radius: 12px;
}

/* Layout */
body {
    display: flex;
    min-height: 100vh;
    background: #f5f6fa;
    overflow-x: hidden;
}

/* Sidebar */
.sidebar {
    width: var(--sidebar-width);
    background: var(--dark-color);
    color: var(--light-color);
    height: 100vh;
    position: fixed;
    left: 0;
    top: 0;
    transition: var(--transition);
    z-index: 1000;
    box-shadow: 3px 0 10px rgba(0,0,0,0.1);
}

.sidebar-header {
    padding: 20px;
    text-align: center;
    border-bottom: 1px solid rgba(255,255,255,0.1);
}

.sidebar-header .logo-icon {
    font-size: 2.5em;
    color: var(--primary-color);
    margin-bottom: 10px;
}

.sidebar-header h2 {
    font-size: 1.3em;
    font-weight: 600;
}

.sidebar-nav {
    padding: 20px 0;
    flex: 1;
}

.sidebar-nav ul {
    list-style: none;
}

.sidebar-nav li {
    margin-bottom: 5px;
}

.sidebar-nav li.nav-divider {
    height: 1px;
    background: rgba(255,255,255,0.1);
    margin: 15px 20px;
}

.nav-item a {
    display: flex;
    align-items: center;
    gap: 15px;
    padding: 12px 25px;
    color: rgba(255,255,255,0.8);
    text-decoration: none;
    transition: var(--transition);
    font-weight: 500;
}

.nav-item a:hover {
    background: rgba(255,255,255,0.1);
    color: var(--light-color);
}

.nav-item.active a {
    background: var(--primary-color);
    color: var(--light-color);
}

.nav-item i {
    font-size: 1.2em;
    width: 24px;
    text-align: center;
}

.sidebar-footer {
    padding: 15px 20px;
    border-top: 1px solid rgba(255,255,255,0.1);
}

.btn-logout {
    width: 100%;
    padding: 10px;
    background: rgba(255,255,255,0.1);
    color: var(--light-color);
    border: none;
    border-radius: 6px;
    cursor: pointer;
    transition: var(--transition);
    font-weight: 500;
}

.btn-logout:hover {
    background: rgba(255,107,53,0.3);
}

/* Main Content */
.main-content {
    flex: 1;
    margin-left: var(--sidebar-width);
    transition: var(--transition);
    background: #f5f6fa;
}

/* Header */
.main-header {
    background: var(--light-color);
    height: var(--header-height);
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 0 30px;
    box-shadow: var(--box-shadow);
    position: sticky;
    top: 0;
    z-index: 900;
}

.header-left {
    display: flex;
    align-items: center;
    gap: 20px;
}

.sidebar-toggle {
    background: none;
    border: none;
    font-size: 1.5em;
    cursor: pointer;
    color: var(--dark-color);
    padding: 5px;
    border-radius: 6px;
    transition: var(--transition);
}

.sidebar-toggle:hover {
    background: var(--accent-color);
}

.main-header h1 {
    font-size: 1.5em;
    color: var(--dark-color);
    font-weight: 600;
}

.header-right {
    display: flex;
    align-items: center;
    gap: 20px;
}

.search-bar {
    position: relative;
    background: var(--accent-color);
    border-radius: 25px;
    padding: 8px 15px;
    display: flex;
    align-items: center;
    gap: 10px;
}

.search-bar i {
    color: var(--gray-color);
}

.search-bar input {
    border: none;
    background: transparent;
    padding: 5px;
    font-size: 0.95em;
    width: 200px;
}

.search-bar input:focus {
    outline: none;
}

.notifications {
    position: relative;
}

.notification-btn {
    background: none;
    border: none;
    font-size: 1.3em;
    color: var(--dark-color);
    cursor: pointer;
    position: relative;
    padding: 8px;
    border-radius: 6px;
    transition: var(--transition);
}

.notification-btn:hover {
    background: var(--accent-color);
}

.notification-badge {
    position: absolute;
    top: -5px;
    right: -5px;
    background: var(--danger-color);
    color: white;
    font-size: 0.7em;
    padding: 2px 6px;
    border-radius: 10px;
    font-weight: 600;
}

.user-menu {
    position: relative;
}

.user-info {
    display: flex;
    align-items: center;
    gap: 10px;
    cursor: pointer;
    padding: 8px 12px;
    border-radius: 6px;
    transition: var(--transition);
}

.user-info:hover {
    background: var(--accent-color);
}

.user-icon {
    font-size: 1.5em;
    color: var(--secondary-color);
}

.username {
    font-weight: 600;
    color: var(--dark-color);
}

.user-role {
    font-size: 0.8em;
    color: var(--gray-color);
}

.user-dropdown {
    position: absolute;
    top: 100%;
    right: 0;
    background: var(--light-color);
    border-radius: 8px;
    box-shadow: var(--box-shadow);
    min-width: 200px;
    display: none;
    z-index: 1000;
}

.user-dropdown a {
    display: block;
    padding: 12px 15px;
    color: var(--dark-color);
    text-decoration: none;
    transition: var(--transition);
    display: flex;
    align-items: center;
    gap: 10px;
}

.user-dropdown a:hover {
    background: var(--accent-color);
    color: var(--primary-color);
}

/* Dashboard Container */
.dashboard-container {
    padding: 30px;
    max-width: 1800px;
    margin: 0 auto;
}

/* Stats Grid */
.stats-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 20px;
    margin-bottom: 30px;
}

.stat-card {
    background: var(--light-color);
    border-radius: var(--card-radius);
    padding: var(--card-padding);
    display: flex;
    align-items: center;
    gap: 20px;
    box-shadow: var(--box-shadow);
    transition: var(--transition);
}

.stat-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.15);
}

.stat-icon {
    width: 60px;
    height: 60px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.8em;
}

.stat-primary .stat-icon { background: rgba(255,107,53,0.15); color: var(--primary-color); }
.stat-success .stat-icon { background: rgba(76,175,80,0.15); color: var(--success-color); }
.stat-warning .stat-icon { background: rgba(255,193,7,0.15); color: var(--warning-color); }
.stat-danger .stat-icon { background: rgba(244,67,54,0.15); color: var(--danger-color); }

.stat-info h3 {
    color: var(--gray-color);
    font-size: 0.9em;
    margin-bottom: 5px;
    font-weight: 500;
}

.stat-number {
    font-size: 2em;
    font-weight: 700;
    color: var(--dark-color);
    margin-bottom: 5px;
}

.stat-change {
    font-size: 0.85em;
    display: flex;
    align-items: center;
    gap: 5px;
}

.stat-change.positive { color: var(--success-color); }
.stat-change.negative { color: var(--danger-color); }

/* Charts Section */
.charts-section {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(500px, 1fr));
    gap: 25px;
    margin-bottom: 30px;
}

.chart-card {
    background: var(--light-color);
    border-radius: var(--card-radius);
    box-shadow: var(--box-shadow);
    overflow: hidden;
}

.chart-header {
    padding: var(--card-padding);
    display: flex;
    justify-content: space-between;
    align-items: center;
    border-bottom: 1px solid #e0e0e0;
}

.chart-header h3 {
    color: var(--dark-color);
    font-size: 1.2em;
    font-weight: 600;
}

.chart-actions {
    display: flex;
    gap: 10px;
}

.btn-sm {
    padding: 6px 15px;
    border-radius: 6px;
    font-size: 0.85em;
    cursor: pointer;
    transition: var(--transition);
    border: none;
    font-weight: 500;
}

.btn-sm.btn-outline {
    background: transparent;
    border: 2px solid var(--gray-color);
    color: var(--gray-color);
}

.btn-sm.btn-outline:hover {
    background: var(--accent-color);
}

.btn-sm.btn-primary {
    background: var(--primary-color);
    color: var(--light-color);
}

.btn-sm.btn-primary:hover {
    background: #FF8C5A;
}

.chart-container {
    padding: 20px;
    height: 300px;
}

/* Recent Activity */
.recent-activity {
    background: var(--light-color);
    border-radius: var(--card-radius);
    box-shadow: var(--box-shadow);
    padding: var(--card-padding);
}

.activity-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
    padding-bottom: 15px;
    border-bottom: 1px solid #e0e0e0;
}

.activity-header h3 {
    color: var(--dark-color);
    font-size: 1.2em;
    font-weight: 600;
}

.view-all {
    color: var(--secondary-color);
    text-decoration: none;
    font-weight: 500;
    font-size: 0.9em;
}

.view-all:hover {
    text-decoration: underline;
}

.activity-list {
    display: flex;
    flex-direction: column;
    gap: 15px;
}

.activity-item {
    display: flex;
    gap: 15px;
    padding: 15px;
    background: var(--accent-color);
    border-radius: 8px;
    transition: var(--transition);
}

.activity-item:hover {
    background: #f0f0f0;
    transform: translateX(5px);
}

.activity-icon {
    width: 40px;
    height: 40px;
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2em;
}

.activity-icon.success { background: rgba(76,175,80,0.15); color: var(--success-color); }
.activity-icon.info { background: rgba(46,134,171,0.15); color: var(--secondary-color); }
.activity-icon.warning { background: rgba(255,193,7,0.15); color: var(--warning-color); }
.activity-icon.danger { background: rgba(244,67,54,0.15); color: var(--danger-color); }

.activity-content {
    flex: 1;
}

.activity-content p {
    color: var(--dark-color);
    margin-bottom: 5px;
    font-weight: 500;
}

.activity-time {
    font-size: 0.8em;
    color: var(--gray-color);
}

/* Responsive */
@media (max-width: 1024px) {
    .stats-grid {
        grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    }
    
    .charts-section {
        grid-template-columns: 1fr;
    }
    
    .search-bar input {
        width: 150px;
    }
}

@media (max-width: 768px) {
    .sidebar {
        transform: translateX(-100%);
    }
    
    body.sidebar-open .sidebar {
        transform: translateX(0);
    }
    
    .main-content {
        margin-left: 0;
    }
    
    body.sidebar-open .main-content {
        margin-left: var(--sidebar-width);
    }
    
    .header-left h1 {
        display: none;
    }
    
    .search-bar {
        display: none;
    }
}

/* Botones adicionales */
.btn {
    padding: 10px 20px;
    border-radius: 6px;
    border: none;
    cursor: pointer;
    font-weight: 500;
    transition: var(--transition);
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
}

.btn-primary {
    background: var(--primary-color);
    color: var(--light-color);
}

.btn-primary:hover {
    background: #FF8C5A;
}

.btn-secondary {
    background: var(--secondary-color);
    color: var(--light-color);
}

.btn-secondary:hover {
    background: #3a9eb8;
}

.btn-success {
    background: var(--success-color);
    color: var(--light-color);
}

.btn-danger {
    background: var(--danger-color);
    color: var(--light-color);
}

.btn-outline {
    background: transparent;
    border: 2px solid var(--gray-color);
    color: var(--gray-color);
}

.btn-outline:hover {
    background: var(--accent-color);
}

.btn-sm {
    padding: 6px 12px;
    font-size: 0.85em;
}

.btn-lg {
    padding: 14px 28px;
    font-size: 1.1em;
}